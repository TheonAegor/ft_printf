#include "../includes/ft_printf.h"

int		ft_proc_c(char *c, int *res,s_modif *flag)
{
/*	
	flag->variable = ft_calloc(sizeof(char), 2);
	flag->variable[1] = '\0'; 
	flag->variable[0] = c; 
*/
//	flag->variable = ft_strdup(c);
//	*res++;
	return (1);
}
